= form_for([@poster, @poster.comments.build], :remote => true, :html => { :id => 'new-comment-form' }) do |f|
  %p
    = f.label 'Add your comment'
    = f.text_area :text , class: "textarea-comment", rows: "2", cols: "40", placeholder: "Type your comment here"
  %p
    = f.submit
